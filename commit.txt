feat(forms): implement encrypted multi-tenant form system

- Introduced Form trait and FormType enum
- Added EncryptedString with Encrypted trait and Encrypter metadata
- Created Submission<T> struct with UUID and timestamp
- Defined FormEntry<T> for typed submission containers
- Planned developer-level multi-tenant form submission model
- Integrated pre-commit Git pipeline for format, lint, and test checks
- Enforced conventional commit message structure via Git hook
