# #!/bin/sh

# RED='\033[0;31m'
# GREEN='\033[0;32m'
# NC='\033[0m' # No Color

# echo "🔧 Auto-fixing formatting..."
# cargo fmt --all

# echo "🔍 Checking formatting..."
# if ! cargo fmt --all -- --check; then
#   echo "${RED}❌ Formatting check failed. Run 'cargo fmt --all' to fix it.${NC}"
#   exit 1
# fi

# echo "🧹 Linting with Clippy (warnings allowed)..."
# if ! cargo clippy --all-targets; then
#   echo "${RED}⚠️ Clippy reported issues (warnings allowed). Review if needed.${NC}"
# else
#   echo "${GREEN}✅ Clippy passed with no hard errors.${NC}"
# fi

# echo "🧪 Running tests..."
# if ! cargo test; then
#   echo "${RED}❌ Tests failed. Fix them before committing.${NC}"
#   exit 1
# fi

# echo "${GREEN}✅ All pre-commit checks passed.${NC}"
